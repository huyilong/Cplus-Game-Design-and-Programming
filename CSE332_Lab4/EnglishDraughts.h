

#include "stdafx.h"
/*
#include <vector>
#include <ostream>
#include <iostream>
#include <sstream>
#include <string>
#include <iomanip>
enum return_value{success, fail, wrongGameName, too_many_input, not_enough_input, unable_to_read_coordinate, invalidMove, stalemateOccured, ninealmonds_user_quit, magicsquare_user_quit, reversi_user_quit, bad_Alloc, unknown_exception, other_exception, one_player_done, one_turn_done };

using namespace std;

class EnglishDraughts
{
	friend ostream &operator<<(ostream &out, const EnglishDraughts &game);
private:
	int width;
	int height;
	unsigned int longestPieceLength;
	vector<vector<string>> gameState;
	string black_player, white_player;
	string current_player;


public:
	EnglishDraughts(string black_player, string white_player);
	~EnglishDraughts(){};

	void print();
	bool done();
	bool stalemate();
	int prompt(unsigned int &fir, unsigned int &sec);
	int prompt(int &inputInt);
	int turn();

	bool hasValidSquare();
	bool checkValid(string current, string opposite);
	bool moveisValid(string current, string opposite, unsigned int player_destination_x, unsigned int player_destination_y);


};


*/